# 2026-02-11 — Session Notes

## Kira App — Massive Build Sprint Complete

### What Got Built (Phases 0-11)
- Full monorepo at `~/kira/kira-app/` — 4 packages (core, api, dashboard, desktop)
- 4-layer SQLite memory engine (34 tests)
- REST + SSE API server on port 3001
- Discord-style chat UI (dark theme #0F1117, accent #7C3AED)
- Dashboard pages: Tasks kanban, VDR browser, Knowledge Graph, Settings, Overview
- Widget system (11 types, CSS-only charts)
- Tauri v2 desktop shell configured
- Onboarding wizard, gamification (XP bar, achievements)
- One-liner installers (PowerShell + bash)
- Git pushed to `https://github.com/clubeedg-ship-it/kira-app` (private)

### Live Deployment
- **URL:** `app.zenithcred.com`
- **Path:** Cloudflare Tunnel → `192.168.0.222:3005` → Docker container port 3001
- **Docker:** `docker-compose.yml` maps 3005→3001, mounts `kira-data` volume + `~/.claude/.credentials.json:ro`
- **Users:** test1 (admin), mark (user)

### AI Provider System
- Built: AnthropicProvider, OpenAIProvider, OllamaProvider, ClaudeOAuthProvider — all pure fetch, no SDKs
- ProviderManager: per-user SQLite, AES-256-GCM encrypted API keys
- Provider resolution chain: user config → env vars → server Claude CLI OAuth → Ollama fallback
- Chat backend: POST /api/chat (SSE streaming), session/message CRUD

### Key Discovery: Anthropic OAuth is NOT Public
- Claude Code uses internal OAuth with client_id `ce61e60a-af57-4c55-ab27-6558a3a0e2b0`
- Token format: `sk-ant-oat01-*` (access), `sk-ant-ort01-*` (refresh)
- Refresh endpoint: `https://console.anthropic.com/v1/oauth/token`
- **Cannot replicate in web app** — undocumented internal API, could break anytime
- Otto wanted each user to auth with their own subscription — not feasible today
- Options: API keys, OpenRouter, or server's Claude Max as default

### Otto's Preferences (Reinforced)
- Design first, code never until design is done (was upset about premature frontend work)
- No external LLM SDKs — pure fetch only
- SQLite only for v1.0
- User installs nothing except Kira
- Revenue ratio: 60% revenue / 30% funding / 10% infrastructure

### Next Steps
1. Verify end-to-end chat works at app.zenithcred.com
2. Add OpenRouter as provider (simplest multi-model option)
3. Tauri native app packaging
4. Per-user token tracking/billing UI
5. Fix Notion VDR agent (`Name` property error)

### ZenithCred Deliverables
- Product design v1.2 (HRV-first positioning)
- Financial projection guide, 10 pilot targets, EEG research
- All in `~/kira/deliverables/`

### Other Deliverables
- OttoGen webinar outline: `~/kira/deliverables/ottogen-webinar-outline.md`
- IAM competitive analysis: `~/kira/deliverables/iam-competitive-analysis.md`

## Night Shift — 21:00 UTC
**Autonomous work session started.** 5 sub-agents spawned:
1. OttoGen Webinar Module 1 outline (Revenue, P1)
2. ZenithCred Financial Projections update (Funding, P0)
3. OttoGen LinkedIn Content Calendar Feb-Mar (Revenue, P1)
4. IAM Free Trial Package (Revenue, P1)
5. Chimera README + ARCHITECTURE (Infra, P1)

All 5 Notion tasks updated to "In Progress".
