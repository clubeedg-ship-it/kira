## Daily Graph Extraction (2026-03-12 02:00 UTC cron)
- Session file: `/home/adminuser/.openclaw/agents/main/sessions/a8172a42-6fcc-4a3e-810d-837191fe46df.jsonl`
- Entity count: 44011
- Relation count: 134393
- New extractions: 0
- NLP enrich: no relevant context found / no new entities, relations, or facts extracted
- Graph improvements: completed (69 entity merges, 23 duplicate relations removed, 146 relations normalized, 882 embeddings cleaned)

## IAM website / deployment notes
- Otto asked to check website completeness; audit found OOPUO site visually strong but not fully launch-ready operationally: contact form only shows success state, booking link placeholder, analytics placeholder, partial HubSpot setup.
- For IAM website, staging was created on this server in `/home/adminuser/projects/iam/staging` with site on port `3008` and isolated chat backend on `127.0.0.1:3861` for testing.
- Otto made clear Ghost is non-negotiable before launch: existing blog content, featured images, clean/professional blog UX, seamless website integration, and secure admin/public routing (likely `blog.interactivemove.nl`) must be finished before launch.
- Confirmed blog page has a real frontend bug causing duplicated bilingual nav labels like `PrijzenPrices`, `Over OnsAbout Us`, `OnderwijsEducation`.
- Confirmed chatbot is broken in user terms: backend exists, but staging `/api/chat` times out / hangs with infinite typing animation.

## IAM website fixes made today
- Fixed text contrast on `prijzen` flexible financing cards by changing headings and subheadings/highlights to dark text in both website and staging CSS.
- Added cache-busting to stylesheet references to make CSS fixes visible despite browser caching.
- Adjusted homepage mobile layout so the `Our Products` / flagship 2-in-1 graphic stacks vertically with text on mobile instead of staying side-by-side.
- Verified current staged video references exist for `hero-home-page.mp4`, `3d-games-hero.mp4`, and `build-park-hero.mp4`, but clarified to Otto that these are only old server copies unless actual new Telegram payloads arrive; identical filenames do not prove the media was replaced.

## Telegram media pipeline issue
- Otto needs to be able to send videos/documents/images by Telegram chat for direct use in projects; this is operationally important and not optional.
- Investigated inbound media storage: normal inbound files land in `/home/adminuser/.openclaw/media/inbound`, but multiple Telegram video/document attachment events from today never persisted there.
- Conclusion: Telegram/OpenClaw attachment events are arriving, but the actual media payloads are not being saved locally for these messages; media-by-chat is currently broken for Telegram video/document flow.
- Temporary workaround suggested: direct download links (Drive/WeTransfer/etc.) until the Telegram media pipeline is fixed.

## VPS / production deployment notes for IAM
- Otto wants host-style staging/production rather than a Docker-first web stack; Docker is optional, not required.
- Recommended production architecture: main site + Ghost + chat backend + SSL + reverse proxy; Ghost should be finished professionally before launch.
- Clarified that Ghost can run without Docker, using local MySQL/MariaDB DB, Node runtime, nginx reverse proxy, and PM2/systemd.
- For developer asks, Otto already has the main domain A record pointing at the server; key remaining needs are working SSH auth and sudo access.
- Tried inspecting the VPS via SSH using both Plesk preview hostname and provided `otto@185.107.90.22` credentials from this environment; host reachable but authentication rejected, likely because password auth is disabled or SSH is key-only.
- One-sentence rationale prepared for Otto: sudo is needed so Ghost and related production services can be installed, secured, proxied, and managed without depending on the developer for every system-level change.

## Worklog / unified DB fix
- Otto noticed an automation message referencing `/home/adminuser/kira/data/unified.db` as empty; verified this was wrong/misleading because the real populated DB is `/home/adminuser/kira/memory/unified.db`.
- Fixed compatibility by preserving the empty stub as `/home/adminuser/kira/data/unified.db.empty-backup-20260312` and replacing `/home/adminuser/kira/data/unified.db` with a symlink to `/home/adminuser/kira/memory/unified.db`.
- Verified the fixed alias path now sees real worklog rows (`worklog` count visible through `data/unified.db`).
- Explained to Otto that the worklog batch exists to convert raw check-ins and execution messages into structured operational memory for reviews, focus tracking, neglected priority detection, and COO-style execution support.

## Night Shift — ZenithCred Funding Prep (21:00+ UTC)
- Queried Notion task queue for Kira-assigned Todo/In Progress items using the canonical IDs file at `~/kira/scripts/workflows/notion-ids.json`.
- Chose a task that could be completed internally without messaging or other external actions: **Finalize investor deck v2 with pilot company data**.
- Audited ZenithCred investor materials and found a credibility problem: some files claimed paying pilots / LOIs while strategy docs explicitly said those did not yet exist.
- Updated `vdr/zenithcred/pitch-deck.md` to **Version 2.0 (March 2026)** with an honest traction revision:
  - replaced fake/fragile traction claims with a pilot-readiness slide
  - added a 3-company pilot data structure (Pilot A/B/C) with buyer, company type, and success metrics
  - clarified what is real today vs what must not be claimed yet
  - updated funding-status and disclaimer language to avoid presenting modeled pilot assumptions as verified customer outcomes
- Updated `vdr/zenithcred/investor-one-pager.md` so traction language matches the revised deck and no longer implies unverified LOIs.
- Marked Notion task **Done**: `310a6c94-88ca-81d9-92f9-c07641a2b6af`.
- Did not message Otto. Summary mirrored to `memory/night-shift-log.md`.
