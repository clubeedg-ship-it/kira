
## CryptPad Setup (21:19 UTC)

Successfully deployed CryptPad on Docker:
- **Ports:** 3003 (main), 3004 (sandbox)
- **Access:** http://192.168.0.222:3003

**Key lesson:** The official CryptPad Docker image requires `CPAD_CONF=/cryptpad/config/config.js` environment variable to be set, otherwise the entrypoint script fails with "cp: cannot create regular file ''" error.

Working docker-compose config saved at `~/cryptpad/docker-compose.yml`.
