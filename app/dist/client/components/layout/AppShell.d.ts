import { type ReactNode } from 'react';
interface AppShellProps {
    children?: ReactNode;
}
export default function AppShell({ children }: AppShellProps): import("react/jsx-runtime").JSX.Element;
export {};
